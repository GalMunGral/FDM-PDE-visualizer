{
  "scripts": {
    "build": "esbuild ./src/index.ts --outdir=dist --bundle",
    "dev": "npm run build -- --watch"
  },
  "devDependencies": {
    "@types/three": "^0.163.0",
    "esbuild": "^0.18.17",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "three": "^0.163.0"
  }
}
